spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/shazamauth
    password: gtahyjump
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
    database-platform: org.hibernate.dialect.MySQL8Dialect


  mail:
      host:
        smtp.gmail.com
      port:
        587
      properties:
        mail:
          smtp:
            auth:
              checked
            starttls:
              enable:
                true
      protocol:
        smtp
      username:
        <adress-email>
      password:
        <key>
